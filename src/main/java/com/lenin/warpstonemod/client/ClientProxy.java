package com.lenin.warpstonemod.client;

import com.lenin.warpstonemod.common.CommonProxy;

public class ClientProxy extends CommonProxy {
	/*
	This is a dummy class for now as the proxy set up function for forge requires both a common and client proxy in order to work
	Without this common proxy wouldn't work

	TO DO: Do we need Common Proxy then??
	 */
}